fn fib(n) {
    if (n <= 1)
        return n;

    return fib(n - 2) + fib(n - 1);
}

fn main() {
    let mut i = 0;

    # This is a comment.

    while (i < 20) : (i += 1) 
        print("fib(" + i + ") =", fib(i));
}
