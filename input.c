int main() {
  printf("Hello, world!\n");
  a = true ? a ? b : c : nullptr;
  break;
  break;
  foo = 0b1010 + 0x10 + 010 + 0b;
  // return 2.43 + .21 + 34.;
  continue;
}

int fib(int n) {
  if (n <= 1) {
    return n;
  }

  foo = 2, 3, (sizeof(thing < -1));

  do
    printf("Hello, world!\n");
  while (fib(n) + 2);

  if (false && 1)
    if (hello)
      printf("what\r");
    else
      &***foo = -4 + sizeof(3);

  return fib(n - 1) + fib(n - 2);
}
