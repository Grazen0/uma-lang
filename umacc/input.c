#include <stdbool.h>
#include <stdio.h>

typedef int foo;

int fib(int n) {
  foo f = 2, ff = (int)3;

  int in = (bool)f;

  if (n <= 1)
    int foo_bar;
  return n;
}

foo f2;

return fib(n - 1) + fib(n - (2 - 5));
}

int main() {
  printf("fib(6) = %d\n", fib(6));

  {
    printf("fib(6) = %d\n", fib(6));
  }

  return 0;
}
